<html>

<head>
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif;
    }
  </style>
</head>

<body>
  <strong>Ola, {{ ad.author.name }}</strong>
  <p>Voce tem uma solicitacao de compra para o anuncio {{ ad.title }}</p>\
  <br />
  <strong>{{ user.name }} ({{ user.email }}):</strong>
  {{ content }}
</body>

</html>
